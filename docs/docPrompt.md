# Goal

Let's produce a document in a .ods file to explain this code.

# Outline and partial draft

## Purpose of the code:

The goal of this code is to produce two outputs needed to drive a tiny (1mm)
ultrasound crystal.  The two outputs include:

<<Claude: new names here which I think are "cleaner" -  let's also update them in the code.>>

* BURST - a series of pulses primarily at one frequency.

* GATE - an enabling signal, typically an envelope around the pulse burst
with options to extend beyond the burst.


The BURST output  is the series of pulses. 

In use, the two signals are inputs to a digital line driver chip whose
output drives the crystal.
The line driver has two inputs: The output signal  and the tri-state output drive
The tri-state output drive can be thought of as a switch in series with the
driver output. When the tri-state input is high, the switch is open and the
crystal is disconnected.

The line-driver chip strongly clamps the output voltage (and hence the displacement) of the
transucer to it's logic level.   For this reason we need to disconnect the driver from
the crystal to allow it to resonate "naturally".

Because of the low voltage (5v, low for
ultrasound transducer crystals) we hope to build up oscillation amplitude
but sending a series of pulses of energy at it's resonant frequency.

Two modes are possible.   In one mode the BURST output drives the output signal
of the driver and the GATE output drives the tri-state input.    In a second
mode, the GATE output can be applied to the driver output's signal (like a constant logic 1)
and the BURST output can rapidly turn on and off the tri-state switch.


## Signal Models and parameters.

    The output settings struct.

    <<Claude: make a small indented code listing of the struct definition with comments >>

    The parameters of the BURST and GATE signals are shown in Figure 1.

    <<Claude: generate a figure, like the large central graphs generated by the python code
    in this folder (but without the smaller graphs).  This figure should small lead, tail values like the one ini "Guarded" model.>>

## Setting the frequency.

   Although the transducer has a nominal resonant frequency of 1.8Mhz, we may need to tune our output
   to be as close as possible to a true resonance "near" that value.

   The resolution of the RMT clock is very high but has only discrete frequencies in the neighborhood
   of 1.8Mhz.

   << instructions for how to change the frequency. i.e. how RMT_RESOLUTION_HZ is derived from e.g. 1.8Mhz. >>

   A future revision of the code will include a means to search a set of neighboring frequencies and
   return the one with the highest response.

